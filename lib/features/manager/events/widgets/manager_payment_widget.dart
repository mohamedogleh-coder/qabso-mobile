import 'package:flutter/material.dart';

class ManagerPaymentWidget extends StatelessWidget {
  const ManagerPaymentWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Manager  Payment");
  }
}
